# Exclude generated code from SonarCloud automatic analysis.
# src/lib/database.types.ts is emitted verbatim by `supabase gen types`
# and CI diffs it against the CLI output, so it cannot be hand-edited
# to satisfy lint findings.
sonar.exclusions=src/lib/database.types.ts
